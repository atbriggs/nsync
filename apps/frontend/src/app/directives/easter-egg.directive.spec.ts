import { EasterEggDirective } from './easter-egg.directive';

describe('EasterEggDirective', () => {
  it('should create an instance', () => {
    const directive = new EasterEggDirective();
    expect(directive).toBeTruthy();
  });
});
